{
  "name": "vestibular-rehab-dashboard",
  "private": true,
  "version": "2.0.0",
  "description": "Vestibular rehabilitation dashboard — component-based build",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && live-server . --port=3000"
  }
}
